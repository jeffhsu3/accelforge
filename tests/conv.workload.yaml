workload:
  shape:
    p: 0 <= p < 5
    r: 0 <= r < 3
    m: 0 <= m < 4
    c: 0 <= c < 2

  einsums:
  - name: conv
    tensor_accesses:
    - {name: I, projection: { C: c, H: p+r }}
    - {name: W, projection: [ m, c, r ]}
    - {name: O, projection: [ m, p ], output: True}
