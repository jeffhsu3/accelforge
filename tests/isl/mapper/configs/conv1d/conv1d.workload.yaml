workload:
  shape:
    r: 0 <= r < 3
    p: 0 <= p < 16

  einsums:
  - name: conv
    tensor_accesses:
    - {name: I, projection: [ r, p ]}
    - {name: W, projection: [ r ]}
    - {name: O, projection: [ p ], output: True}
